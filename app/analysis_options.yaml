# ============================================
# FILE: app/analysis_options.yaml
# ============================================
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # TODO(TICKET-001): Re-enable once all top-level functions are typed.
    #   Owner: <assign_owner>  |  Target: <set_sprint/date>
    strict_top_level_inference: false

    # -- Safety & Correctness -----------------------------------------
    avoid_print: true
    avoid_dynamic_calls: true
    avoid_slow_async_io: true
    avoid_returning_null_for_void: true
    avoid_void_async: true
    cancel_subscriptions: true
    close_sinks: true
    unawaited_futures: true
    use_build_context_synchronously: true
    no_duplicate_case_values: true
    literal_only_boolean_expressions: true
    throw_in_finally: true
    unnecessary_statements: true

    # -- Immutability & Const -----------------------------------------
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_final_in_for_each: true

    # -- Style & Readability ------------------------------------------
    always_use_package_imports: true
    prefer_single_quotes: true
    sort_child_properties_last: true
    require_trailing_commas: true
    use_super_parameters: true
    sized_box_for_whitespace: true
    use_colored_box: true
    use_decorated_box: true
    avoid_unnecessary_containers: true
    prefer_is_empty: true
    prefer_is_not_empty: true

    # -- Async Hygiene ------------------------------------------------
    unnecessary_await_in_return: true

    # -- Performance --------------------------------------------------
    noop_primitive_operations: true

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true

  errors:
    # Treat these as hard errors -- never ignore at review.
    unawaited_futures: error
    close_sinks: error
    cancel_subscriptions: error
    use_build_context_synchronously: error
    avoid_slow_async_io: warning
    avoid_void_async: warning

  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
